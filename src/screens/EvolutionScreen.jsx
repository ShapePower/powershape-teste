import Wrapper from "../components/Wrapper.jsx";

const EvolutionScreen = ({ history }) => {
  return (
    <Wrapper className="evolution" history={history}>
      <p>Evolution</p>
    </Wrapper>
  );
};

export default EvolutionScreen;
